# model setting
wavelet: morlet
max_scale: 2
nb_orients: 4
efficient: true

# training setting
dataset: curet
classifier_epochs: 10
conv_epochs: 10
batch_size: 200
lr: 0.001
weight_decay: 1e-5
train_ratio: 0.5
